spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: testpassword
  jpa:
    generate-ddl: off
    show-sql: false
logging:
  group:
    com.github.vendigo.grabdeezer: debug

update.period.days: 7